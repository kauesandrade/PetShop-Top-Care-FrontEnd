export interface ProductVariantResponse {
    variantTitle: string,
    variantCode: number,
    price: number,
    discountPrice: number,
    parcels: number,
    stock: number,
    images: Array<any>
}
